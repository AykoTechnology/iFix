# docs/api

`openapi.json` gerado automaticamente a partir dos schemas Zod de `src/api` via `zod-to-openapi` (ADR-009). **Não editar manualmente** — este diretório é saída de build, versionada para permitir diff de contrato em PRs.
