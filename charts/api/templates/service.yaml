apiVersion: v1
kind: Service
metadata:
  name: {{ include "ifix-api.fullname" . }}
  labels:
    {{- include "ifix-api.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "ifix-api.selectorLabels" . | nindent 4 }}
