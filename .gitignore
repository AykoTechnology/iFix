# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
# Exceção: os artefatos de design são VERSIONADOS de propósito. O gate 10 compara o
# compilado com o que está no repositório — se não estiver versionado, não há com o
# que comparar, e o gate reprova por ausência em vez de por divergência.
!design-system/dist/
build/
.next/
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Testing
coverage/

# Editor
.vscode/*
!.vscode/extensions.json
.idea/

# OS
.DS_Store
Thumbs.db

# Supabase local
supabase/.branches
supabase/.temp

# Storybook
storybook-static/
storybook-static/
